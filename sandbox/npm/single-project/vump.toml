# One npm package, versioned on its own.
#
# Git is off throughout the sandbox. These projects have no repository of their
# own — they sit inside vump's — so a commit or tag made here would land in it.
# "none" is a decision rather than silence, so an interactive run does not
# offer to commit either.
#
# A flag can override that setting, so two layers stand behind it. vump refuses
# git work from a configuration nested inside another's repository unless
# --allow-nested is passed. And the names below mean that a tag made in spite
# of all that still cannot pass for a release: the release workflow triggers on
# `v*`, and the defaults here would have been "v{new_version}" and "bump
# version to v{new_version}".

files = ["package.json", "package-lock.json"]

[git]
through = "none"
tag_pattern = "sandbox-npm-v{new_version}"
commit_message = "chore(sandbox): bump npm/single-project to {new_version}"
